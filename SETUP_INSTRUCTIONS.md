# 🚨 تعليمات هامة لرفع المشروع على GitHub

## ⚠️ مشكلة تسجيل الدخول والحل

### 📋 المشكلة:
بعد رفع المشروع على GitHub، قد تواجه مشكلة "اسم المستخدم أو كلمة المرور غير صحيحة" لأن:
1. localStorage يتم مسحه عند فتح الملف من GitHub Pages
2. المستخدم الافتراضي لا يتم إنشاؤه تلقائياً في بعض الحالات

### ✅ الحل الفوري:

#### **1. بيانات الدخول الافتراضية:**
```
👤 اسم المستخدم: المدير
🔑 كلمة المرور: admin123

أو

📧 الإيميل: admin@institute.edu
🔑 كلمة المرور: admin123
```

#### **2. إذا لم تنجح بيانات الدخول:**
افتح وحدة التحكم (F12) والصق هذا الكود:
```javascript
// إنشاء المستخدم الافتراضي يدوياً
const defaultAdmin = {
    id: 'admin_default',
    name: 'المدير',
    email: 'admin@institute.edu',
    password: btoa('admin123'),
    role: 'admin',
    status: 'active',
    permissions: {
        add_students: true,
        edit_students: true,
        delete_students: true,
        export_excel: true,
        import_excel: true,
        manage_departments: true,
        manage_users: true,
        view_statistics: true,
        block_students: true,
        backup_restore: true
    },
    createdDate: new Date().toLocaleString('ar-SA'),
    lastLogin: null,
    createdBy: 'System'
};

// حفظ المستخدم في localStorage
let users = JSON.parse(localStorage.getItem('users')) || [];
users.push(defaultAdmin);
localStorage.setItem('users', JSON.stringify(users));

console.log('تم إنشاء المستخدم الافتراضي بنجاح');
location.reload();
```

## 🔧 مراجعة الملفات الرئيسية

### ✅ الملفات التي تم التحقق منها:

1. **index.html** - الملف الرئيسي ✅
   - بيانات الدخول: المدير/admin123
   - جميع الميزات تعمل
   - نظام الصلاحيات مكتمل

2. **student_search.html** - صفحة البحث للطلاب ✅
   - تعمل بشكل مستقل
   - لا تتطلب تسجيل دخول

3. **create_excel_template.html** - قالب Excel ✅
   - الروابط محدثة إلى index.html

4. **create_excel_template_v2.html** - قالب Excel احترافي ✅
   - يعمل بشكل مستقل

## 🚀 خطوات الرفع على GitHub

### 1. تأكد من الملفات:
```
HISTJS/
├── index.html                    # ✅ الملف الرئيسي
├── student_search.html           # ✅ صفحة البحث
├── create_excel_template.html    # ✅ قالب Excel
├── create_excel_template_v2.html # ✅ قالب Excel احترافي
├── README.md                     # ✅ التوثيق
└── *.xlsx                        # ✅ قوالب Excel
```

### 2. رفع على GitHub Pages:
1. ادفع الملفات إلى GitHub
2. فعّل GitHub Pages من Settings
3. اختر main branch ومجلد root

### 3. اختبار بعد الرفع:
1. افتح الرابط من GitHub Pages
2. جرب بيانات الدخول الافتراضية
3. إذا لم تنجح، استخدم الكود أعلاه

## 📞 الدعم الفني

### إذا واجهت مشاكل:
1. تأكد من فتح الملف الصحيح: `index.html`
2. امسح localStorage من المتصفح
3. استخدم كود الإنشاء اليدوي أعلاه
4. تحقق من وحدة التحكم للأخطاء

## 🔐 ملاحظات أمنية

### بيانات الدخول:
- **المدير الافتراضي**: admin123
- **يُنصح بتغييرها** بعد أول تسجيل دخول
- يمكن إضافة مستخدمين جدد من إعدادات المدير

---

**🎓 المعهد العالي للعلوم والتقنية الجفرة بسوكنة**
**📅 آخر تحديث: 2026**
