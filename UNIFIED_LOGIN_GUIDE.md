# 🔐 دليل توحيد بيانات الدخول والمواد

## 📋 المشكلة الحالية

### **النسخة المحلية (الكمبيوتر)**:
- 👤 **اسم المستخدم**: admin
- 🔑 **كلمة المرور**: admin123

### **النسخة المرفوعة (GitHub)**:
- 👤 **اسم المستخدم**: المدير
- 🔑 **كلمة المرور**: admin123

## ✅ الحل الموحد

### **بيانات الدخول الموحدة**:
الآن النظام يدخل كلا المستخدمين تلقائياً:

```
👤 اسم المستخدم: admin
🔑 كلمة المرور: admin123

أو

👤 اسم المستخدم: المدير
🔑 كلمة المرور: admin123

أو

📧 الإيميل: admin@institute.edu
🔑 كلمة المرور: admin123
```

## 🔧 التغييرات التي تم تطبيقها

### **1. إنشاء مستخدمين افتراضيين**:
```javascript
// المستخدم الافتراضي الرئيسي
{
    id: 'admin_default',
    name: 'admin',
    email: 'admin@institute.edu',
    password: btoa('admin123'),
    role: 'admin'
}

// مستخدم التوافق
{
    id: 'manager_default',
    name: 'المدير',
    email: 'manager@institute.edu',
    password: btoa('admin123'),
    role: 'admin'
}
```

### **2. تحسين تحميل المواد المخصصة**:
```javascript
function loadCustomSubjects() {
    const customSubjects = localStorage.getItem('departmentSubjects');
    if (customSubjects) {
        const parsed = JSON.parse(customSubjects);
        Object.assign(departmentSubjects, parsed);
        console.log('📚 Loaded custom subjects:', Object.keys(parsed));
    }
}
```

### **3. تحسين استيراد الإعدادات**:
```javascript
// Load custom subjects after import
loadCustomSubjects();
```

## 🚀 خطوات الاستخدام

### **للاستخدام المحلي**:
1. افتح `index.html`
2. استخدم: admin / admin123

### **للاستخدام على GitHub**:
1. افتح الرابط من GitHub Pages
2. استخدم: المدير / admin123
3. أو استخدم: admin / admin123

### **لاستيراد المواد**:
1. سجل الدخول بأي مستخدم
2. اذهب إلى "استيراد وتصدير Excel"
3. اختر "استيراد إعدادات"
4. ستظهر المواد المخصصة تلقائياً

## 📊 التحقق من العمل

### **رسائل Console**:
عند فتح الصفحة، تحقق من console للرسائل التالية:
```
✅ تم إنشاء المستخدم الافتراضي بنجاح
👤 اسم المستخدم: admin
🔑 كلمة المرور: admin123
✅ تم إنشاء مستخدم المدير للتوافق
👤 اسم المستخدم: المدير
🔑 كلمة المرور: admin123
📚 Loaded custom subjects: ["التمويل والمصارف", "المساحة"]
```

## 🔍 استكشاف الأخطاء

### **إذا لم تظهر المواد بعد الاستيراد**:
1. تحقق من console لرسائل `📚 Loaded custom subjects`
2. أعد تحميل الصفحة
3. جرب تسجيل الدخول مرة أخرى

### **إذا لم تنجح بيانات الدخول**:
1. امسح localStorage من المتصفح
2. أعد تحميل الصفحة
3. جرب بيانات الدخول المختلفة

## 📝 ملاحظات هامة

### **التوافق الكامل**:
- النظام الآن متوافق مع كلتا النسختين
- المواد المخصصة تعمل في كلتا النسختين
- بيانات الدخول موحدة

### **الأمان**:
- كلا المستخدمين لديهما نفس الصلاحيات
- كلاهما يستخدم نفس كلمة المرور
- يمكن تغيير البيانات لاحقاً من إعدادات المدير

---

**🎓 المعهد العالي للعلوم والتقنية الجفرة بسوكنة**
**📅 آخر تحديث: 2026**
